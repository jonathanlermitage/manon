   ### DEV CONFIGURATION for local unit testing ###  (warning: avoid tabs, indent with two spaces)

spring:
  data:
    mongodb:
      database: manondev
      username: root
      password: woot
  redis:
    database: 5

manon:
  security:
    bcrypt:
      strength: 4
